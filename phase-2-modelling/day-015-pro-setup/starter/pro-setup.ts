export function firstAbove(numbers: number[], threshold: number): number | undefined {
  // TODO: return the first number greater than threshold, or undefined if none is
  throw new Error("not implemented yet");
}

export function firstAboveOrDefault(numbers: number[], threshold: number, fallback: number): number {
  // TODO: call firstAbove; if it gives back undefined, return fallback instead
  // TODO: compare with === undefined, not a truthiness check, so a real 0 is kept
  throw new Error("not implemented yet");
}
