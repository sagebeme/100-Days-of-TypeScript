# Copy this file to .env (in this same folder) and put your real values in it.
# .env is ignored by git, so your key never ends up on GitHub. This example file is committed.
RATES_API_URL=http://localhost:4545/latest
RATES_API_KEY=replace-me-with-your-key
