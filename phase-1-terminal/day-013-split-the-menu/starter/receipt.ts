import { findItem } from "./menu.ts";

export function orderTotal(itemNames: string[]): number {
  // TODO: for each name, look it up with findItem
  // TODO: if it comes back undefined, throw new Error(`Unknown item: ${name}`)
  // TODO: add up the prices and return the total
  throw new Error("not implemented yet");
}
