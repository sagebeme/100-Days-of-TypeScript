export function countAttemptsUntilCorrect(guesses: string[], answer: string): number {
  // TODO: declare `let attempts = 0` and `let index = 0`
  // TODO: while (index < guesses.length) { ... }
  // TODO:   attempts += 1
  // TODO:   if (guesses[index] === answer) return attempts
  // TODO:   index += 1
  // TODO: return -1 (nobody guessed right)
  return 0;
}
