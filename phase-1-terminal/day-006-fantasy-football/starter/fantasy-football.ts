export function calculateFantasyPoints(
  players: { goals: number; assists: number; cleanSheet: boolean }[]
): number {
  // TODO: declare `let total = 0`
  // TODO: for (const player of players) { ... }
  // TODO:   total += player.goals * 4
  // TODO:   total += player.assists * 3
  // TODO:   if (player.cleanSheet) total += 4
  // TODO: return total
  return -1;
}
