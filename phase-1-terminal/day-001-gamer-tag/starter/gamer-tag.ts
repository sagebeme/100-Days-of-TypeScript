export function buildGamerTag(name: string, favoriteNumber: number): string {
  // TODO: declare a `const prefix` holding "xX_"
  // TODO: declare a `const suffix` holding "_Xx"
  // TODO: declare a `let tag` starting as `${name}${favoriteNumber}`
  // TODO: reassign `tag` to `${prefix}${tag}${suffix}`
  // TODO: return `tag`
  return "";
}
