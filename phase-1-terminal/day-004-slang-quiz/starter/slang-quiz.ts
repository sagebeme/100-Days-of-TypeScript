export function pickRandomWord(words: string[], random: () => number = Math.random): string {
  // TODO: turn random() into a valid index: Math.floor(random() * words.length)
  // TODO: return words[index]
  return "";
}
